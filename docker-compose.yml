services:
  bot:
    build: .
    container_name: mathgen-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_uploads:/app/uploads
    dns:
      - 8.8.8.8
      - 1.1.1.1
volumes:
  bot_uploads:
